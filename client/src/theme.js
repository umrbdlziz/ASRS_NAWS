// src/theme.js
import { createTheme } from "@mui/material/styles";

const theme = createTheme({
  palette: {
    primary: {
      main: "#192832", // Change this to your primary color
    },
    secondary: {
      main: "#2D465A", // Change this to your secondary color
    },
  },
});

export default theme;
